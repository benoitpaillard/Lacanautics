name: Build vector bathymetry v4.2
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'tools/build_vector_v42.py'
      - '.github/workflows/build-vector-v42.yml'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pillow numpy scipy scikit-image cairosvg
      - run: python tools/build_vector_v42.py
      - name: Show vector QC
        run: cat data/lacanau_vector_v42_report.json
      - name: Validate inline JavaScript
        run: |
          python - <<'PY'
          import re, pathlib
          s=pathlib.Path('hires.html').read_text()
          blocks=re.findall(r'<script>(.*?)</script>',s,re.S)
          pathlib.Path('/tmp/lacanautics-inline.js').write_text('\n'.join(blocks))
          PY
          node --check /tmp/lacanautics-inline.js
      - name: Validate vector assets
        run: |
          test -s bathymetry-geopdf-v42-zones.svg
          grep -q 'depth-ge-7' bathymetry-geopdf-v42-zones.svg
          grep -q 'lacanautics-v4.2-vector' sw.js
          grep -q 'VECTOR 4.2' hires.html
      - name: Commit vector app
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add bathymetry-geopdf-v42-zones.svg data/lacanau_vector_v42_report.json hires.html index.html sw.js manifest.webmanifest
          git diff --cached --quiet && exit 0
          git commit -m "Add filled vector GeoPDF bathymetry v4.2"
          git pull --rebase origin main
          git push
